# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
commands:
  print_pipeline_id_command:
    description: prints pipeline id
    steps:
      - checkout
      - run:
          name: prints outs the unique pipeline id
          command: echo $CIRCLE_WORKFLOW_ID
jobs:
  configure_infrastructure:
    docker:
      - image: python:3.7-alpine3.11
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints: ["40:ca:00:61:d4:59:ba:20:5a:58:93:a0:9f:31:bb:68"]
      - run:
          name: Install Ansible
          command: apk add --update ansible
      - run:
          name: Run playbook and configure server
          command: ansible-playbook -i inventory main.yml --private-key ansible_pair.pem 


# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - configure_infrastructure
